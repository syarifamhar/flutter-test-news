import 'package:flutter/material.dart';

class HotNewsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hot News'),
      ),
      body: Center(
        child: Text('Hot News Screen'),
      ),
    );
  }
}
