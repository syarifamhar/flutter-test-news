import 'package:flutter/material.dart';

class SubscriptionInfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Subscription Info'),
      ),
      body: Center(
        child: Text('Subscription Info Screen'),
      ),
    );
  }
}
